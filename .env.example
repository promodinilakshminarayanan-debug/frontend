# Base URL of your deployed Go backend (no trailing slash).
# For local development against a backend running on your machine:
VITE_API_BASE_URL=http://localhost:8080

# Once deployed to AWS, point this at the live App Runner/ECS/EC2 URL:
# VITE_API_BASE_URL=https://your-app-runner-url.awsapprunner.com
